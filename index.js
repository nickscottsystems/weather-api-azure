// Express-based server removed. Use Azure Functions in the `wunderground/` folder.
// This file is intentionally left as a placeholder to avoid accidental startup.

/*
If you previously used `npm start` to run the Express server, switch to the
Azure Functions host instead:

  func start

The HTTP-trigger function is in `./wunderground` and will handle `/api/wunderground`.
*/

module.exports = {};
